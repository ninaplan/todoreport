# V2 아이디어 & 백로그

## 굵직한 업데이트
- 할일 보관 기능 — 인박스(수집함)와 함께 구현. 카테고리 보관과 별개
- 인박스/수집함 — 날짜 선택이 선택사항인 투두
  - **길 A (권장 검토):** `date` 옵셔널화 + 노션 날짜 속성 비움과 동기화. 앱 인박스 ↔ 노션 「날짜 없는 페이지」. 캘린더·일별 목록은 `date != nil`만. 마이그레이션·기존 equals 조회·리포트 relation 영향 범위 큼
  - **길 B:** 앱만 인박스(로컬 플래그), 노션 push 시 오늘/지정일로 강제 — 구현 단순, 노션과 의미 불일치
- 할일 순서 수동 편집 (드래그)
- 반복 투두 변경/취소 처리 (alert 플로우)
- 언어 선택 (한국어/영어) — 전체 String Catalog 작업 필요
- AI 주간 요약 (Claude API, Vercel 타임아웃 이슈로 이관)
- iCloud 백업/기기 이전
- 카테고리-Notion 프로젝트 DB 연동
- 동적 속성 매핑 (relation 타입 지원)
- 플래너 데이터 이전 툴
- 위젯 인터랙티브 체크·+ 버튼
- 스와이프 액션 버튼 순서 커스터마이징 — 끝까지 당기면 실행되는 자리를 사용자가 직접 지정
- 삭제 후 되돌리기(Undo) 토스트 — 스와이프 삭제 확인창 없이 빠르게 삭제, 대신 일정 시간 내 복구 가능하게
- Tombstone(묘비) 패턴 — 삭제된 항목이 stale한 pull 응답으로 부활하는 이슈 방어. Undo 토스트와 함께 묶어서 구현 검토 (2026-07-08 조사)
- ~~달력 「이 달 불러오기」~~ — **v1.08 완료** (백엔드 범위 GET + iOS 달력 불러오기 아이콘, 날짜 이동 자동 pull 없음). 캘린더 탭 확장 시 동일 API·컴포넌트 재사용
- **노션 삭제 → 앱 반영** — v1.0.7에서 pull 부재 삭제를 제거한 트레이드오프. Notion `page.deleted` 웹훅 → 백엔드 → 앱 tombstone/삭제. (아래 §미해결 상세)

## 자잘한 업데이트
- 앱 전체 문구 톤앤매너 통일 (~요 → ~입니다 체)
- 이모지/기본아이콘 SF Symbol로 전면 교체 (다크모드 대응)
- 플래너 아이콘·색상 커스터마이징 — 사용자가 색상 팔레트·아이콘을 직접 선택 (2026-07-11: 신규 플래너 랜덤 색상 자동배정 시도했다가 되돌림. 현재 SF Symbol 아이콘 세트가 플래너와 무관한 것들이 많아 세트 자체 교체도 필요)
- **카테고리 팔레트 해금** — v1.08은 9세트 전부 무료. 이후 챌린지(누적 완료/스트릭/시즌)로 세트 해금하는 UX 검토. 해금은 영구. 해금 상태는 iCloud 또는 백엔드 동기화 필요(기기 이전·재설치 대비)
- **카테고리 커스텀 색·아이콘** — `ColorPicker`→hex 저장, 이모지 우선(SF Symbol 검색은 중간 작업). 이미지 아이콘 비권장. 팔레트 세트 자동 재배색(`recolorCategories`)과 충돌 → 커스텀 색 카테고리는 재배색에서 보존하는 규칙 필요
- **데일리 리포트 카드 접기/펼치기** — 홈(투두 탭) 상단 완료율 카드. **1.09** 후보. 리포트 탭 `ExpandableCompletionCard` 패턴 참고, 펼침 상태는 `@AppStorage`로 저장
- **미선택 칩 배경 불투명도 조정** — `FilterChip` 미선택 `color.opacity(0.12)`가 옅은 팔레트에서 티가 안 남 → 불투명도·채도 미세조정
- 설정에서 앱 포인트 컬러 선택 기능 (노크오렌지 외 Apple 기본 컬러)
- 노크오렌지 과다 사용 정리
- 구독 섹션 카드 디자인 개선
- 설정 화면 빌드번호 제거, 내부 로그에만 기록
- 피커 폰트 회색 처리
- 별점 명칭/아이콘 대체
- 투두 고정 스와이프 애니메이션 개선
- TodoRow 시간 표시 (scheduledTime 있는 항목)
- 달력 UX 미세조정 — 월 이동 시 선택 해제, 날짜·목록 폰트, 말풍선 위치·삼각형 간격 (범례 가로 스크롤·불러오기 아이콘은 v1.08 반영)

## 기술 개선
- TodoService → DataRepository 패턴 통합
- 영어 로컬라이제이션 (String Catalog)

## V3
- 플래너 간 Notion 템플릿 데이터 마이그레이션

## Notion 토큰 끊김 관련 조사 기록 (2026-06-24)

### 결론
- Notion access_token에는 시간 기반 만료가 없음 (Notion 정책 확인)
- ~~토큰이 끊기는 진짜 원인: 같은 Notion 워크스페이스를 다른 디바이스/빌드에서 다시 OAuth 인증(재연결)하면, Notion이 기존 access_token과 refresh_token을 "동시에" 무효화함~~
- ~~curl로 재현 테스트 시 invalid_grant 에러로 확인됨~~
- ~~"워크스페이스 1개 = 플래너 1개" 원칙이 사실상 강제되는 이유가 이것~~
- ~~재인증 시 이전 토큰 자동 무효화~~
- → **정정 (2026-07-10):** 재인증 자체는 안전함 (Notion 동의 화면에서 기존 페이지 체크를 유지한 경우). 실제 문제는 두 가지였음:
  (1) 앱의 "연동초기화"가 명시적으로 revoke API를 호출하면 워크스페이스 공유 권한 전체가 끊김 (Notion이 사용자+Integration당 권한 상태를 하나만 관리하기 때문)
  (2) Notion OAuth 재동의 화면에서 기존에 체크돼 있던 페이지를 사용자가 직접 해제하면 그 페이지 접근이 즉시 빠짐 (앱 코드가 관여 못 하는 영역, Notion 자체 동작)
  NotionWorkspaceConnection 구조(1단계+2단계, 2026-07-10 커밋 15c9a98)는 (1)을 막는 안전장치로 여전히 유효함 — 마지막 참조 플래너가 아니면 revoke 자체를 스킵함. (2)는 코드로 방지 불가, OAuth 진입 전 안내 문구로만 완화 가능.

멀티플래너 UI 복원 완료 (2026-07-11 기준 정상 노출·사용 중). 이후 플래너 순서변경·삭제 관리 화면도 추가됨 — CLAUDE.md "플래너 관리" 섹션 참고. 워크스페이스 안내 방식도 재검토 중: 기존 무조건 노출되던 인라인 문구를 조건부 alert("페이지 선택 시 주의해주세요")로 교체하는 작업 설계 완료, 적용은 다음 세션.

### 처리 방향
- 이미 구현해둔 refresh token 인프라(저장/전파)는 롤백하지 않고 유지
  - 이유: 향후 Notion이 시간 기반 만료 정책을 도입하거나 토큰 정책이
    바뀔 경우를 대비한 자산으로 판단
- ~~현재 버그(워크스페이스 중복 연결)는 refresh로 해결되지 않음 — 별개 문제~~
- 관련 코드에는 "원인은 시간 만료가 아니라 중복 OAuth"라는 설명 주석이
  있었음 (2026-06-26, 백엔드 lib/notion-auth.ts, refresh route,
  iOS PlannerItem.swift) — 2026-07-10 정정 내용 반영 예정

### 향후 검토 항목
- refresh token 인프라의 실질적 효용 재평가 — 코드가 6개 파일에 분산되어
  있어 유지보수 부담이 있음. 당장 제거하지는 않되, 추후 사용 사례가
  명확해지지 않으면 정리 검토
- ~~워크스페이스 중복 연결 자체를 막는 방어 로직 (아직 미설계) — 예: 같은 workspace_id로 재연결 시도 시 경고/차단~~ → NotionWorkspaceConnection(15c9a98)으로 동일 워크스페이스 토큰 공유·revoke 안전장치 구현 완료
- ~~"워크스페이스 1개 = 플래너 1개" 원칙과 멀티유저 공유 기능(V2)의 아키텍처 충돌 — 별도 설계 결정 필요~~ → 동일 워크스페이스 멀티 플래너 허용으로 방향 전환, UI 복원 완료 (2026-07-11)

## Sync 근본 구조 개선 조사 기록 (2026-07-08)

### 완료된 것
- Notion 응답에 lastEditedTime 필드 추가 (백엔드 GET/POST/PATCH)
- iOS notionLastEditedTime 필드 추가 (Todo, TodoItem)
- Push 성공 시 응답의 lastEditedTime을 로컬에 저장 (SyncQueueProcessor)
- Pull upsertFromNotion에 Version Guard 추가 (stale 응답 스킵)
- Debounce pull에 push 큐 대기 로직 추가

### 미해결(후속) — relation 지연으로 노션발 항목 편집 반영 지연 (2026-07-15 조사)
- 증상: 노션에서 새로 생성되어 앱으로 내려온 할일을 노션에서 편집하면, 새로고침을 여러 번 해야 반영됨. 앱에서 원래 있던 할일은 1회에 반영.
- 원인: insert 직후 `notionRelationLinked == false` → 매 pull마다 relation update enqueue → `hasPendingOperation`이 pull 갱신 전체 차단. relation PATCH 성공 시 `notionLastEditedTime`이 내부 작업 시각으로 올라가 버전 가드가 낡은 GET을 연속 스킵.
- 수정 방향 후보: (A) relation 이미 enqueue됐거나 linked면 재enqueue 억제, (B) relation 성공 시 `notionLastEditedTime` 갱신하지 않기(사용자 편집 시각 오염 방지).

### 미해결(후속) — 노션에서 삭제한 할일이 앱에 남음 (v1.0.7 의도된 트레이드오프)
- v1.0.7에서 pull 부재 삭제를 제거해 삭제-재생성 레이스·중복 버그를 막음. 노션 웹에서 직접 삭제한 항목은 앱에 남을 수 있음.
- 근본 해결: Notion `page.deleted` 웹훅 → 백엔드 신규 서브시스템 → 앱 tombstone/삭제 처리. 별도 세션 2~3개 분량 예상.

### 미해결(후속) — 포그라운드 5분+ 복귀 시 카테고리 sync 이중 호출
- `TodoReportApp` `scenePhase.active`의 `CategoryNotionSync`와 `syncFromNotion` 내부 `syncNotionCategoriesIfNeeded`가 겹침. 기능상 문제 없으나 여유 시 한쪽으로 통합 검토.

### 미해결(V2) — Tombstone 부재
- 시나리오: 새 투두 A 생성 → 즉시 삭제 → 이탈했다가 복귀 → A 부활
- 원인: 백엔드 캐시(25초 TTL) 안에서 pull 응답이 "A 살아있던 시절" 상태를 내려줌.
  로컬에 삭제됐으니 신규 insert 분기 진입 → A 부활
- Version Guard로 못 잡힘: Guard는 "기존 로컬이 있을 때" 덮어쓰기 방어용.
  로컬에 없는 항목이 부활하는 케이스는 대상 밖
- 심각도: UX 이슈(최대 25초 유령 항목). 데이터 손실 없고 자연 회복됨
- V2 처리 방향: RecentlyDeletedTracker (notionPageId + 삭제시각) 저장소 도입 →
  upsertFromNotion 신규 insert 분기에서 최근 삭제 pageId 스킵 → 24시간 후 자동 정리.
  Undo 토스트 기능과 함께 묶어서 구현 검토

### 미해결(V2) — Backend 캐시 정합성
- Vercel Edge Function이 여러 인스턴스로 분산 → 메모리 캐시가 서로 안 맞는 문제
- Redis(Upstash 도쿄 리전) 프로젝트 연결까지는 완료. lib/cache.ts 교체는 미착수
- 오늘 도입한 Version Guard가 클라이언트 방어망 역할을 하므로 Redis 근본 해결의 시급성은 낮아짐
- 다만 성능/latency 관점에서 Redis 도입은 여전히 유효. 별도 세션 예정
